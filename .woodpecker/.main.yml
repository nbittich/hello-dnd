steps:
  run-hello-world:
    image: nestybox/ubuntu-jammy-systemd-docker:latest
    commands:
      - dockerd &
      - docker run hello-world
    backend_options:
      docker:
        runtime: sysbox-runc
        hostname: syscont
when:
  - event: push
    branch: master
