steps:
  run-hello-world:
    image: docker:latest
    commands:
      - dockerd &
      - docker run hello-world
    backend_options:
      docker:
        runtime: sysbox-runc
        hostname: syscont
when:
  - event: push
    branch: master
