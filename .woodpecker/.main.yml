steps:
  run-hello-world:
    image: docker:latest
    commands:
      - dockerd &
      - docker run hello-world
    # backend_options:
    runtime: sysbox-runc
when:
  - event: push
    branch: master
